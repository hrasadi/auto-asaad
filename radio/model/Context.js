const Context = {
    Defaults: {
        Publishing: {
            SocialListeningProps: {
                DefaultLife: 24,
                MaxLife: 48,
                VoteBonus: 0.5,
            },
        },
    },
};

module.exports = Context;
