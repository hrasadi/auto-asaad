const UserManager = require('../../../collaborativelistening/UserManager');
class Raa1UserManager extends UserManager {
    registerUser(user) {

    }

    updateUser(user) {

    }
}

module.exports = Raa1UserManager;
